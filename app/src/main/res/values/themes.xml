<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Snapchat" parent="Theme.Material3.Light.NoActionBar">
        <!-- Customize your light theme here. -->
         <item name="colorPrimary">@color/yellow</item>
        <item name="colorPrimaryVariant">@color/yellow</item>
        <item name="android:statusBarColor">@color/yellow</item>
        <item name="colorOnPrimary">@color/yellow</item>
    </style>

    <style name="Theme.Snapchat" parent="Base.Theme.Snapchat" />

    <style name="Theme.App.SplashScreen" parent="Theme.SplashScreen" >
        <item name="windowSplashScreenBackground">@color/yellow</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/splashsnap</item>
        <item name="postSplashScreenTheme">@style/Base.Theme.Snapchat</item>
    </style>
</resources>